# How to run
1. Install npm (search nodejs installation on the brower, download and run it)
2. Open terminal
3. Type "cd [file path to this project folder]" and press enter
3. Type "npm install" and press enter
4. Type "npm run dev" and press enter
5. Copy the local host link shown and paste into your browser and press enter




(Autogenerated on project creation below)
# React + TypeScript + Vite + shadcn/ui

This is a template for a new Vite project with React, TypeScript, and shadcn/ui.

## Adding components

To add components to your app, run the following command:

```bash
npx shadcn@latest add button
```

This will place the ui components in the `src/components` directory.

## Using components

To use the components in your app, import them as follows:

```tsx
import { Button } from "@/components/ui/button"
```
